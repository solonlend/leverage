# Solon 杠杆 LP — 暴力测试/红队报告(2026-09-04)

结论先行:**本轮修掉 3 个真问题,20 万次随机调用不变量全绿,已知攻击类重放全部挡住。**
这提高置信度,不等于"无漏洞"——上主网前仍建议外部审计(尤其 LpShareOracle 估值与 Extra fork 交界)。

## 测试面

| 层 | 内容 | 结果 |
|---|---|---|
| 单元/集成 | 64 条(金库流程、事件、SwapExecutor、真借贷池集成、fork e2e) | 64/64 ✅ |
| 不变量 fuzz | 2000 轮 × depth 100 = 200,000 次随机 存/取/开/加/平/领/清算/时间/价格 | 5/5 不变量 ✅ |
| 数学 fuzz | FairLpMath/FeedSqrt/Settlement 差分 vs Python golden + 5000 轮 fuzz(含推价操纵不动公允价) | ✅ |
| 攻击重放 | 通胀捐赠、恶意 zapPath、重入、零流动性垃圾、尘量清算 | 6/6 ✅ |

5 条不变量:出借人偿付能力 / 金库不滞留资金 / 债务记账自洽 / 凭证-仓位一致 / eToken 汇率单调不减。

## 本轮抓到并已修复

1. **[中] 开仓/加仓无健康检查**(keeper 演练时发现):可秒开可清算仓位,把出借人押在 ~101% 抵押率上。
   修复:open/increase 后 `require(_isHealthy)`,UNHEALTHY_OPEN/UNHEALTHY_INCREASE。
2. **[低] harvest 复投分支滞留零头**(invariant fuzz 抓到):zap/increase 取整尘留在金库。
   修复:compound 分支补 `_refundDust`。
3. **[低] 零流动性垃圾仓位**(A4):1 wei 开仓可铸空仓位刷 debtId/事件。修复:open 加 `ZERO_LIQ` 卫。

## 攻击重放结论

- **A1 通胀捐赠**:偷不走钱(Extra 官方修复有效,burn 1000→dead)。残留面:大额捐赠把 exchangeRate
  整数除法压 0,可 DoS 整个储备的存款(攻击者净亏 ~99.9% 捐赠)。
  **缓解已落地**:部署脚本强制种子存款(A1b 证明种子后免疫)。**种子存款是上线硬前置,不可跳过。**
- **A2 恶意 zapPath**:用户可控路径被 SwapExecutor 首尾端点校验拦截(PATH_IN/PATH_OUT/PATH_LEN)。
- **A3 重入**:zap 中反调金库被 nonReentrant 拦截(实测 revert 理由 = REENTRANCY)。
- **A5 尘量清算**:允许执行但取整方向对协议有利,keeper 不可套利、只能拿走尘量价值。
- **推价操纵(Alpha Homora 类)**:公允价来自 Chainlink 交叉,spot 操纵不影响估值
  (FairLpMath fuzz `test_spotManipulation_doesNotMoveFairPrice`);开仓被夹由 minLiquidity 拦。

## 经济与运维残留风险(未修,需知情决策)

1. **中心化权限**:LendingPool owner 可 initReserve/改授信/冻结;金库 GOVERNOR 收协议费、管清算白名单。
   上线前应移交多签(Extra 主网同样做法)。
2. **keeper 单点**:清算白名单制,keeper 挂掉=没人清算。建议 ≥2 个独立 keeper + 告警。
3. **金库内部 swap minOut=0**:滑点防护依赖调用方参数(开仓 minLiquidity、平仓 minOutSingleToken、
   清算 minSeizeOut)。前端必须默认带上;keeper 已带。
4. **SwapExecutor 单池依赖**:默认路由 fee100 WETH/USDG 一个池,池子流动性迁移时需换 executor(金库不可升级,
   SWAP_EXECUTOR 是 immutable —— 届时要重部金库或预留 registry,记入 v1.5 议题)。
5. **利率参数**:沿用 Extra 默认曲线((80%,20%)→(90%,50%)→(100%,150%),储备费 15%),
   未按 RH 实际资金成本校准,上线后按利用率观察调参(setBorrowingRateConfig 是 owner 权限)。
6. **StakingRewards 已知坑**(Extra 文档):setReward 前置校验弱、claim 可被单币卡死。v1 不启用 staking
   激励,启用前先按官方 Code Fix 核对。

## 第二轮:纯提钱攻击(Exploit.t.sol,黑帽视角 · 胜利条件=攻击者期末>期初)

7 个提钱向量,每个断言"提不出钱":

| # | 攻击 | 结果 |
|---|---|---|
| E1 | 开满杠杆→崩价→抢在清算前自平甩坏账 | 攻击者**净亏**(甩不出利润),✅ |
| E1b | 坏账透明度 | **发现**:自平水下留孤儿坏账 2750 USDG,totalBorrows 虚高掩盖出借人损失 → 已加 `BadDebt` 事件可观测 |
| E2 | keeper+borrower 自清算薅 bonus | 攻击者净亏 1032 USDG(bonus 是自己抵押,还倒贴协议抽成),✅ |
| E3 | 循环开平 50 次抠舍入零头 | 无净利,✅ |
| E4 | 直接 close 绕过 harvest 抽成 | 协议照样 skim,✅ |
| E5 | eToken 转账+双方赎回 | 总赎回 ≤ 存款,✅ |
| **E6** | 向金库误转/捐赠→open+close 整包薅走 | **真洞,已修** |

### 本轮修复
- **[中] E6 捐赠薅取**:`_refundDust` 退的是金库全部余额(假设"交易间不持币"),攻击者向金库转 1 万 USDG 后 open+close 即整包扫走(实测薅走 9999.99)。修复:每个变更函数入口快照余额,只退"当前-基线"的增量,预存资金永不被扫。两金库同修,120k 次不变量复测无回归。
- **[信息] E1b 坏账可观测**:close/liquidate 全平后若债务仓仍有残债,emit `BadDebt(id, debtId, residual)`,供监控告警。坏账本身是借贷固有风险(缓解=LLTV 缓冲+及时清算),不阻断(阻断会让水下用户无法平仓、坏账更大)。

## 第三轮:深层攻击面(Exploit2.t.sol)

| # | 攻击 | 结果 |
|---|---|---|
| F1 | 真预言机 LpShareOracleV4 喂坏数据(过期/零/负/未答round/roundId=0) | 全部 fail-closed revert,✅ |
| **F2** | 清算过度扣押(预言机低估 posVal 时 keeper 超额扣押) | **真洞,严重,已修** |
| F3 | keeper 超付 repayAmount | 被 closeFactor 封顶,金库不吞钱,✅ |
| F4 | 同块循环部分清算 | 受健康度约束,不能清光,✅ |
| F5 | 债务指数 warp 110 年 | 单调增无溢出/回绕,✅ |

### 本轮修复
- **[严重] F2 清算过度扣押**:`toLiquidator = gotLoan - protocolFee` 用的是**实际卖出所得**,从不封顶到 SettlementMath 算出的应得 `s.liquidatorSeize`。当预言机低估仓位价值(快速行情下 Chainlink 滞后、或单 feed 打嗝)时,`seizeLiq` 被按低估的 posVal 放大,keeper 扣押并卖出远超应得 bonus 的抵押。**实测:还款 4000、应得 bonus 288,却净薅 3168 USDG(11 倍超额),差额全从借款人抵押里出。** 修复:keeper 净得封顶到 `s.liquidatorSeize`,**超出部分先冲抵借款人剩余债务(降坏账)、余额退还借款人本人**。修复后同场景 keeper 净得精确 288。两金库同修,200k 次不变量复测无回归。
  - 注:keeper 是白名单准入,但此洞让**被攻陷/作恶的 keeper 或利用预言机滞后**都能超额榨取借款人,属必修。

## 第四轮:访问控制 / 跨仓位隔离(Exploit3.t.sol)

G1 非持有人不能 close/increase/harvest 别人仓位(onlyHolder)· G2 非白名单不能 liquidate · G3 操作自己仓位不动别人债务/流动性 · G4 重复平仓/平已销毁仓 revert · G5 攻击者向金库灌币不能卡死受害人平仓(refundDust 修复后)· G6 debtId 绑定 position 无法跨仓混淆。**6/6 全过,无发现。**

## 第五轮:组合攻击 / 外部恶意合约(Exploit4.t.sol)

针对"单点无害、构造起来出漏洞"的担忧,写了恶意合约做原子多步攻击:

| # | 组合攻击 | 结果 |
|---|---|---|
| H1 | ERC777 式回调代币作风险腿,金库转手续费触发 hook,合约在回调里重入 close | 被 `REENTRANCY` 卫挡住,✅ |
| H2 | 三明治夹金库自身 zap swap | minOut=0 时受害人平仓只拿回 2500(应 ~5000);带滑点下限则 tx 保护性 revert,✅ |
| H3 | 单合约一笔 tx:open→自清算→close 原子套利 | 净额不为正,✅ |

**结论**:跨函数重入(即使经由恶意回调代币)被共享 `nonReentrant` 锁挡死;原子组合无法凭空生利。**唯一需要外部纪律的是 H2 三明治**——滑点下限(open 的 minLiquidity / close 的 minOutSingleToken / liquidate 的 minSeizeOut)是唯一防线,前端与 keeper 必须默认带非零值(见残留风险 #3,现有 H2 具体数字佐证:无保护时被夹掉 ~50%)。

## Dual-Borrow 轮(2026-09-04 下午,当天架构切换后全部重打)

| 层 | 内容 | 结果 |
|---|---|---|
| 单元/集成 | V4 dual 19 条 + V3 dual 红队 5 条 + oracle/exactOutput 各 3-4 条 | ✅ |
| 红队 | D1-D8(捐赠/自清算/循环/抽成/重入/尘量/代还/坏账逃逸)+ V3D 套件 | ✅ |
| **深度性质 fuzz** | P1 平仓结算(漂移±60% × 杠杆 × 比例):坏账⇒用户零提款、不滞留、revert 原子性 — **25,000 组**;P2 清算封顶(漂移 × 预言机错价±30% × 任意k):**F2 恒成立** — **25,000 组** | ✅ |
| **百万次不变量** | 双储备 handler 升级为连贯动价(PM/SV/汇率/预言机同 tick,缺口/降级/坏账路径全参战)+ topUp 平仓 + delta rebalance;5 铁律 × **1,000,000 次调用** | ✅ |
| 全真 e2e | RH 主网 fork:真池开→补→换区间→真 exactOutput 补缺口平仓;真喂价折算;真双储备借还 | ✅ |

### Dual 轮修复的真洞(3 个)
1. **[中] repay 实际额记账泄漏**:6 个还款点忽略 repay() 返回值,利息取整差致资金滞留(百万级不变量逼出)。
2. **[中] exactOutput 前漏 approve**:缺口买入主路径必败、静默降级为全额换币(更差执行);连真链 e2e 都被降级路径掩盖,V3 红队抓出。已修并用测试钉死主路径。
3. **[低] GAP_EPS 假缺口**:liquidity 取整尘误触发缺口机制,加尘埃容忍。

### 价值守恒抽查
自清算全连招(open→自砸→liquidate→close)攻击者净流 = **精确 −协议费**,一分不多。

## 异构审查轮(2026-09-04,四路独立视角,KousanS 点名要求)

四路:①全新上下文 Solidity 安全审计 agent ②经济机制审计 agent ③设计-代码一致性审计 agent ④内置 code-review(high)。三路独立交叉确认同一条致命链,并各自贡献独有发现。

### 【致命,已修】C1/E-1:水下平仓降级分支 = 出借人提款机(三路 CONFIRMED)
组合:降级 exactInput 的 minOut 写死 0 + zapPath 只验首尾、中间跳任意 + exactOutput(反向编码)与降级(正向编码)用同一份 path 导致可**确定性触发降级** + 全平免任何事后检查。攻击者用正向编码的垃圾路径,把本应还债的整条盈余腿洗进自己部署的池子,债务甩成 BadDebt 由出借人承担;另有 gas 饿死、真池自夹两个等效触发面。**一个波动日可按铺仓规模等比掏空储备。**
修复(组合拳):①降级换币强制空 path 直连默认池(禁用户路由)②minOut 由预言机公允价反推(MAX_GAP_SLIPPAGE_BPS=5%)③全平留残债必须先过"预言机作证平仓前已资不抵债"(SOLVENT_BAD_DEBT 闸,连带堵死喂价停摆窗口逃逸=M3/E-4)④回归测试:恶意路径平仓终态与诚实平仓逐字段等价。

### 【高,已修】E-2:开仓多借未用退款 = 借款变现口子
多借的币退进钱包、债务全留 → 等于通用抵押借贷 + 跨储备 carry 套利(借 5% 的 WETH 存 38% 的 USDG 池)。修复:开仓零头**先按腿冲抵各自债务**(借而未用自动回池),剩余(自带部分)才退用户;真链 fork 实测未用的 491 USDG 自动还回。

### 【中,已修】其余代码修复
- M1+decimals:预言机构造器强制 loanFeed∈{feed0,feed1} + 三 feed 小数位一致(fail-open 错配=WETH 债按 1 USDG 计价的灾难)。
- 储备绑定:构造器校验 reserve 底层代币 == 两腿代币(id 写反=记账灾难,code-review PLAUSIBLE)。
- SEIZE_VALUE_LOW:清算赎回总价值不得低于预言机口径应扣押值的 95%(防清算 tx 被夹/预先推价吃掉还债与退款份额)。
- E-5:清算收全仓 fee 补抽成(与 close 口径一致);DUST_LIQ 粉尘清算门槛;L5 构造器参数上界;M5 清算尾款收款失败落国库(黑名单不可 DoS 清算);S-2 孤儿坏账开放任何人代还;L9 registry 两步转移;L1 LiquidityAmounts safe-cast;M6 旧 TWAP 预言机打"禁止部署"横幅。

### 【部署闸】V4 版 Permit2(code-review CONFIRMED)
真 v4-periphery PM 走 Permit2 结算,V4 dual 当前直接 approve 会在 SETTLE_PAIR revert(mock 掩盖)。**V4 列 v1.5 部署闸**;v1 部署目标 UniV3DualVault 不受影响。

### 【判定为误报/接受】
- S-5"换窄区间虚增抵押":生产预言机按"数量×喂价"估值,区间中性(mock valPerLiq 才有该假象)。
- 双金库代码重复(DualVaultCore 抽象):真实维护风险(漏 approve 即因此只在一版被先发现),列 v1.5 重构;当下纪律=任何结算修复必须双版同落+双套件重跑。
- 经济结构项(利率双储备解耦、LLTV 按债务构成分档、closeFactor 收敛式清算、USDG 脱锚断路器、坏账 writeOff 与第一损失资本):列为 v1.5 与上线参数决策,写入 DEPLOY-CHECKLIST。

### 为什么此前五轮+百万级 fuzz 没抓到 C1
自测的 mock swap 永远按公允价诚实兑付 —— minOut=0 在"诚实世界"里不可见;我写的测试继承了我写实现时的同一套假设。异构审查的价值恰恰是不共享这个假设(经济审计员的第一问就是"这笔 swap 的受益人是谁")。结论:**fuzz 强度不能替代威胁模型多样性**,外部审计依然必要。

复测:153/153 + 双储备不变量 20 万次 + 深度性质 2 万组全绿。

## Sepolia 真链暴打战役(2026-09-04,公共测试网实弹)

部署:Sepolia 官方 Uniswap V3(Factory/NFPM/SwapRouter02,fee100) + mock WETH/USDG(自由 mint) + MockFeedOwned 可控喂价 + 双储备 LendingPool + UniV3DualVault 全套。地址见 `broadcast/DeploySepoliaDual.s.sol/11155111/`;金库 0x2D8C63d9f90807A7febB04300B7E5c4997BAC559。harness=`script/BeatSepolia.s.sol`(storm/churn/drift/crash/adversarial/donate/reseed/audit 八波)。

**262 笔真实交易**。价格路径:$2500→$908(-64%)→$4755(+424%),全部用真 swap 推池价+喂价同步(池-预言机一致)。战果:
- 冒烟 e2e:开(双借)/补/换区间/全平,残债 539 wei(GAP_EPS 内)。
- **双向清算都真实触发**:跌向 pos4 部分清算(closeFactor 50%,债 10495→5081)→继续砸到资不抵债(债>值)→4 轮清到底;涨向 pos2/pos3 连环清算(WETH 债务升值面,验证价值口径清算两方向对称工作)。
- **坏账全链路**:插水后孤儿坏账正确隔离(0.087W+175.7U / 0.115W+46U),第三方 addMargin 代还清零(S-2 实弹验证),出借人分文未损。
- 敌意断言(链上态模拟):恶意 zapPath 对出借人无损、陌生人清算/清算健康仓/动他人仓全拒。
- 捐赠攻击实弹:5000 USDG+1 WETH 直打金库,平仓者卷不走(E6 基线快照)。
- 终局账目:USDG 储备 500,000.0018(≥种子,含息),WETH 对应;金库残留=捐赠款分毫不差。
- 实弹观察:E-2 修复让开仓自动修剪超借,实际杠杆被压低——双借结构对价格漂移显著抗清算(两腿自对冲),真正危险面在"出区间+单向漂移"。
- harness 自身修了两个 bug:砸价打穿流动性带致 MAX_TICK+价格换算平方溢出(修复=swap 带目标价 sqrtPriceLimit 硬限+reseed 波);ADV-A 断言初版写反(无缺口时恶意 path 未被使用是合法结果,正确断言=拒绝或对出借人无损)。
- V4 版未上链打:Permit2 部署闸(v1.5),改造后用同一 harness 重打。

## V4 Permit2 改造 + 真链战役(2026-09-04 下午,原 v1.5 部署闸提前拆除)

用户确认 RH 链 V4 池有真实 TVL(ETH/USDG v4 $815万等)后拍板补完 V4。改造与验证:
- **Permit2 结算落地**(src/UniV4DualVault.sol):_armPermit2 = token→Permit2 万能授权 + Permit2.approve(token,PM,max160,max48),_v4Mint 前上膛;Permit2 无代码的链(单测 mock 环境)自动跳过,mock 兼容的直接 ERC20 授权只在无 Permit2 时保留(审计 F2);新增治理紧急阀 revokePermit2(审计 F1,清零额度+复位重上膛)。
- **同款潜伏洞连带修复**:单币版 UniV4LeverageVault 同样缺 Permit2(规格审查路的 adjacent finding),同步落同款修复。
- **EIP-170 超限**(真广播才暴露,测试不查体积):V4 dual 26,037B 超限 1.4KB → 运行时 require 字符串全部转 custom errors(21 处) + TickMath/LiquidityAmounts 外置为 external 库 LpMathExt(delegatecall) → 23,823B(余量 753B)。部署清单新增:记录 LpMathExt 库地址、cast codesize 验证目标链 canonical Permit2。
- **异构复审两路**(独立 agent):规格路对 action 字节/参数元组/PositionInfo 布局/StateLibrary slot6/Permit2 签名/poolId 派生逐项 VERIFIED-CORRECT;安全路结论"设计成立"(Permit2 额度只对 PM 生效+真 PM 付款人=调用者本人+金库跨交易余额≈0 三重兜底),建议 F1/F2/F4 全部采纳。
- **fork 真链验收 3/3**:RH 主网真 V4 PM 全 e2e(open 即 SETTLE_PAIR 经 Permit2)+ 显式断言金库 Permit2 额度已上膛 + **反证测试**(vm.etch 抹掉 Permit2 后 open 必 revert,证明无兜底路径)。
- **Sepolia 真链 V4 战役(~88 笔)**:官方 v4 PoolManager/PositionManager/StateView/PoolSwapTest;V4 金库注册为共享借贷池的 vaultId=2(多金库共存实证);风暴 3 仓→$4755→$1398(-70%,双借对冲跌向再次实证无清算)→拉到 $9841(+107%)涨向 pos1/pos2 连环清算至全平(真 v4 PM 上 decrease/burn TAKE_PAIR)→孤儿坏账第三方代还清零→敌意断言 4/4→捐赠攻击无损。终局:USDG 储备 500,000.0018(与 V3 战役共享同池,双战役后仍≥种子),金库残留=捐赠款分毫不差。

## 补充场景实弹(2026-09-04,用户点名"公开攻击方式都模拟了吗")

**Sim A 预言机停摆(fail-closed 语义链上实测,V3 金库)**:ETH 喂价打成 2 天前旧时间戳后——open=StalePrice 拒、liquidate=StalePrice 拒、需要估值的部分平仓拒;**全额干净平仓仍放行**(还清两腿债、无缺口、不伤出借人,设计如此——停摆不困住能全额还款的用户;留残债的全平被 SOLVENT_BAD_DEBT 挡)。语义闭环。
**Sim B USDG 脱锚 $0.85(链上实测,双喂价连贯)**:oracle 立即重定价(1 ETH $9840→11,577 USDG 口径);WETH 债重的 V3 仓位应声破线、USDG 债重的 V4 仓位仍健康(方向正确);脱锚状态下清算一次成功、账目正确;孤儿粉尘代还后归零。恢复 $1 后全体检干净。
**坦白未模拟(与用户同步过)**:①Chainlink minAnswer 地板价类(Luna/Venus)——mock 喂价无 minAnswer,真栈风险在 v1.5 脱锚断路器;②MEV 夹用户平仓——合约有 minOut 参数,填值靠前端默认纪律;③fee-on-transfer/rebasing 奇葩币——v1 只上标准 WETH/USDG,列上新币硬检查;④治理钥匙被盗——多签+时间锁运营纪律,非代码可模拟。

## 【真链实证】粉尘缺口量纲缺陷(v1.2 → v1.3,2026-09-04 夜)

**发现路径**:用户要求"动过合约就整套重测",在 v1.2 的 Sepolia 重打战役中,V4 普通全平 revert(空 data)。三组只读对照定位:
① topUp 覆盖缺口 + 禁 swap → 成功 ② 不 topUp + 禁 swap → SlippageGap(正确) ③ 默认路径(允许 swap)→ revert。

**根因**:`GAP_EPS = 1e3` 以**绝对 wei**参与比较。对 6 位小数的 LOAN(USDG)是 0.001 刀的合理粉尘线;对 **18 位小数的 RISK(WETH)腿等于 1e-15 刀,形同虚设**。预估与执行之间累积的几 gwei 利息粉尘(实测 shortRisk = 7,555,334,671 wei ≈ $0.0001)因此被判为"真缺口",逼出一笔粉尘 exactOutput → 失败 → 降级路径试图砸掉全部 2152 USDG 盈余 → 整笔 revert。**用户默认路径下平仓失败**(资产不丢,补一点点币即可平,但属必修可用性缺陷)。

**为什么此前所有测试都没抓到**:mock 环境两个代币都是 18 位小数,量纲缺陷在 18/18 世界里根本不存在;生产是 18/6。与"mock 永远诚实兑付"同属一类 —— **mock 与生产的量纲/假设不一致 = 结构性盲区**。

**修复(V3/V4 同步)**:新增 `_riskIsDust(amt)`,RISK 腿的缺口与残债一律经预言机折成 LOAN 价值再比 GAP_EPS;`amt==0` 短路以保住"喂价停摆下干净平仓仍放行"。全平后的残债/坏账判定同步改价值口径(否则粉尘残债会撞 SOLVENT_BAD_DEBT 安全闸)。安全语义不变:超过粉尘线的任何真实残债照样被拦。

**接受的副作用**(用户确认:"粉尘就像无常损失一样,都是精度产生的不可避免的"):平仓后借贷池可能留下价值 < 0.001 USDG 的残债,任何人可用 addMargin 代还。

**判别性测试**(成对):`test_V3FIX_dust_gap_does_not_force_swap`(价值 $0.00075 的缺口必须零 swap 且平仓成功)+ `test_V3FIX_real_gap_still_settles`(价值 $2.5 的真缺口必须照常一笔 exactOutput)。**实证判别力**:把修复临时退回旧逻辑 → 粉尘测试立即变红、真缺口测试仍绿;恢复后双绿。为此在 mock PM 上加了 `setShortfall0` 确定性构造粉尘缺口(第一版测试写出来是空转的,shortRisk 读出来是 0,已推翻重写)。

**真链闭环验证**:v1.3 新仓同样出现粉尘缺口(68,451,360 wei);同一笔全平调用 v1.2 revert / v1.3 成功(tx `0x46dd4b83842e2b21a70da5b236b3f531cc71837b0ca2d96da7e699b6aba5221f`,仓位销毁,用户取回 1,984.76 USDG)。

**同批次其他改动**(均已 153/153 + fork 9/9 + 20 万次不变量 + 万组 fuzz 重跑):harvest(claim/复投)与 increase(杠杆加仓)从单币版移植回 dual 双版(重写时遗漏,用户抓出);V4 一个降级分支漏落 E-1 修复已补(判别测试 D9);V4 体积重构(V4Encode/liqSplit/dues/previewClose 外置 + settleGap 双分支参数化合一)。

## 复现命令

```bash
forge test                                                # 71 tests
FOUNDRY_INVARIANT_RUNS=2000 FOUNDRY_INVARIANT_DEPTH=100 \
forge test --match-contract InvariantsTest                # 20万次调用不变量
forge test --match-contract AdversarialTest -vv           # 攻击重放(通胀/zapPath/重入/尘量)
forge test --match-contract ExploitTest -vv               # 纯提钱向量(坏账/自清算/舍入/捐赠薅取)
forge test --match-contract Exploit2Test -vv              # 深层(预言机对抗/过度扣押/超付/清算循环/百年利率)
forge test --match-contract Exploit3Test -vv              # 访问控制/跨仓位隔离/griefing
forge test --match-contract Exploit4Test -vv              # 组合攻击(回调代币重入/三明治/原子自清算)
# ── Dual-Borrow 轮 ──
forge test --match-contract 'DualVaultTest|ExploitDualTest|ExploitDualV3Test'   # dual 单元+红队
FOUNDRY_FUZZ_RUNS=25000 forge test --match-contract DualDeepFuzzTest            # 深度性质 5万组
FOUNDRY_INVARIANT_RUNS=5000 FOUNDRY_INVARIANT_DEPTH=200 \
forge test --match-contract InvariantsDualTest                                  # 百万次调用不变量
forge test --match-contract RhDualForkTest -vv                                  # RH 真链全真 e2e
```

## 【v1.3 压缩版真链战役】Sepolia,2026-09-04 夜

**为什么打**:v1.2→v1.3 改了合约(`_riskIsDust` 价值口径 + harvest/increase 移植 + V4 E-1 补齐),按"动合约=整套重测"的纪律,链上战役必须重打。测试币余额只够压缩版(用户拍板:跑压缩版),故每波取代表性场景而非全量,**这一点如实记录,不当作等价于八波全量**。

### V3(0x6fce1e0fC407D1d9a1214C1B13EF6360F8c84a12)
| 波次 | 结果 |
|---|---|
| 前置 | 补 200 万 USDG 回储备 1(D 批"全部提取"测试把可用现金提空,利用率 99.97%,不补则 USDG 腿借不到) |
| storm N=3 | #7/#8/#9 开出,杠杆 1.90x/2.01x/2.28x,宽窄带混合 |
| churn | #7 #8 换区间、#9 补保证金,全成功 |
| drift down 12% | $8,003 → $7,042,喂价同步 |
| drift up 25%×2 | $8,803 → $11,004,**三仓全部仍健康**(+56% 单向涨幅打不穿 → 双借自对冲实证) |
| drift up 30%×2 | $14,306 → $18,505,#7 #8 先不健康,第二步 #9 跟进 |
| crash | 实弹清算 9 笔:#7 #8 各 3 轮至全平,#9 部分清算回到健康(closeFactor 循环正确) |
| audit | 抓出孤儿坏账:#7 残 0.02266 WETH、#8 残 0.01958 WETH(≈$419/$362 @ $18,505) |
| 坏账代还 | addMargin 代还两笔;WETH 储备可用流动性 199.4209 → 199.4632,**+0.0423 WETH 与坏账额分毫不差** |
| 极端全平 | #9 在 $18,505 + 深度出区间下默认路径全平成功(tx `0x0c66673881aa590c0b00870d47257af68e1115af1112062f5f512a0e6b6a0d57`)——v1.3 粉尘修复在极端态同样成立 |
| donate | 5,000 USDG + 1 WETH 直打进金库:卡在金库,进不了任何仓位、无人可卷走 |
| adversarial | ADV-A/B/C/D 四条拒绝路径全过(恶意 path 中和 / 陌生人清算被拒 / 健康仓不可清算 / 陌生人平仓被拒) |
| 终局 audit | live=0,残留仅 #3 4.6e-10 WETH、#10 1.6e-11 WETH(已认可的精度粉尘) |

**关于坏账**:30% 一跳往上推、中间无清算人接力,是刻意制造的跳空场景;清算把可动用价值全部变现后仍有缺口 = 设计内的坏账,不是漏洞。当前 v1 没有 writeOff/socialize,孤儿坏账只能靠 addMargin 代还(已实证可行),这一条属 v1.5 待办。

### V4(0x105e019b288307455822863CdC2E6B03BdFF347F)
| 波次 | 结果 |
|---|---|
| 坑 | **两个金库共用一个喂价**:V3 池被推到 $18.5k 后喂价跟着走,V4 池还在原价 → V4 开仓全被拒。对齐喂价到 V4 池价后恢复。生产是同一个真预言机不存在此问题,但测试网多池共享 mock feed 是结构性陷阱,记录备查 |
| storm N=2 | #2 #3 开出 |
| churn | #2 补保证金、#3 部分平仓 |
| adversarial | 四条拒绝路径全过 |
| drift up 30%×2 | $12,664 → $16,464,#3 转不健康 |
| crash | #3 清算 2 轮回到健康 |
| donate | 同样卡死在金库 |
| 终局 audit | #2 #3 均健康;孤儿坏账仅 id1 2.05e-10 / id4 1.7e-11 WETH(粉尘);**V4 侧无真实坏账** |

**gas**:0.0398 → 0.0157 ETH。

**运维事故(非合约)**:跑到 V4 donate 后本机 RPC 全断,`Can't assign requested address (os error 49)`。定位=常驻的 morpho-blue-liquidation-bot(pid 40202)单进程占 12,037 个 socket,吃光端口池。与 2026-08-06 同类。教训进 [[feedback-cleanup-ephemeral-servers]]。

### 与八波全量的差距(欠账,如实列)
- 未跑 reseed 波;storm 只开 3+2 个仓而非 6+;drift 步数少,没有覆盖"下跌向清算"(本轮只打穿了上涨向)。
- V4 未做坏账代还实证(本轮 V4 没产生真实坏账)。
- 补齐需要更多测试币。

## 【异构复审轮 · codex】前端 + 积分索引器,2026-09-04 夜

**为什么做**:用户接入 codex CLI(独立额度)后,第一次真正的跨引擎复审。当天新写的三块——runTx 全站接线、双腿利息口径、`points/farm_points.py` 计分模型——**代码和测试都出自同一只手**,属于结构性盲区(与 09-04 白天 C1/E-1 那次同一类问题:自测继承实现者假设)。

**方法**:给 codex 的 prompt 自包含(讲清 dual-borrow 架构、两腿小数位、每块代码的**意图**),但**不给任何结论、不说我认为哪里没问题**;要求只报能指出行号与触发条件的缺陷,找不到就写"无发现"。

**结果:10 条,其中 2 条是自测永远抓不到的真洞。**

### 致命 1:积分可被无限刷(P-1)
`farm_points.py` 把 `PositionOpened` / `PositionIncreased` 里的 `borrowRisk`/`borrowLoan` 当本金记账。但 `UniV3DualVault.sol` 里 `_repayOpenDust`(open 246 行 / increase 286 行附近)会把"借了没用掉"的部分**当场还回**,而随后 emit 的仍是 `p.borrowRisk` 这个**请求值**。
攻击:故意单腿超额借款 → 合约立刻自动退还(攻击者近乎零成本)→ 索引器按虚高数字长期计分 → **无限刷分**。
**修复**(codex 执行):不信金库事件金额,改索引借贷池自身 `Borrow`/`Repay`。池事件不含 debtId,解法=池日志与金库事件同处一笔交易且金库总是"先借还、后 emit",故按"同交易中 logIndex 大于该池日志的第一个金库终结事件"反查仓位,再用 `PositionOpened` 的 debtRisk/debtLoan 映射到腿;配不上就 `raise`,宁可炸不静默错配。取数按两个 indexed 地址位过滤为本金库(`[topic, None, vault, vault]`),避免共用借贷池的 V4 金库日志混入——**这一条是我复核时单独验证的,codex 未主动说明**。
**判别数字**:真实链上 position 7 持有一天,旧 2901.9114 分 → 新 1450.9557 分;合成超借 1000 → 100 分。

### 致命 2:revert 被当成成功(F-1)
`tx-toast.ts` 在回执 `status === "reverted"` 时弹了错误 toast,**却仍 `return hash`**。所有调用方以 `if (!h) return;` 判成功 → 上链但执行失败的交易被当成功:多笔流程继续发下一笔,单笔流程把失败交易记成"最新成功交易"。当天全站 15 处写操作全部受影响。
**修复**:reverted 返回 `undefined`。

### 其余 8 条(均已修)
- 防重失效:按钮只靠 `useWriteContract().isPending`,该状态在拿到哈希那刻结束,而 runTx 还在等回执 → 窗口内可重复点击造成重复加仓/还款/存款。修复=新增 `hooks/use-busy.ts`(**ref 上锁**,只靠 state 拦不住同一渲染内的连点),12 个写按钮接入。
- 债务 RPC 读失败静默回退 `0n` → 有债仓位显示零债务/零利息/错误加权利率。修复=`debtsLoaded` 门控,失败显占位符。
- **利率/价格未加载时回退到 8% 常数** → 在真实 USDG 利率 149% 时显示一个精确到小数点后一位、却完全错误的收益率。修复=显示"－"。**教训:回退到"看起来合理"的默认值比显示空更危险**,因为它不留任何"这是估算"的痕迹。
- 跨环境污染:`use-reserve-rates` 读 Sepolia 借贷池,而 Farm 列表展示 RH 主网池 → 主网净 APY 被测试网利用率牵着走。**用户裁定:调试阶段不处理,挂进"上主网前必须处理"清单。**
- ETH 价格未加载时把 ETH 保证金当 0(权益/债务构成/利息全漏 ETH 腿却仍显示成有效数字)、提现路径 `awaitAllowance` 返回值被忽略、Increase 在喂价未加载时静默早退无反馈、积分负 delta 跨仓位串账(每仓 `running` clamp 了,写进持有人总账的却是未截断的原始负值)。

### 回归验证(全部我自己跑)
`forge test` 164/164 全过(含 4 个 fork 套件真打 RH 主网);UI 机器人 A+C 40/40、B 批 6/6、D 批 4/4;`points/test_farm_points.py` 2/2;索引器真打 Sepolia 全量跑通无异常。

### 方法论收获
1. **`forge script --skip-simulation` 的日志不是链上成功的证据**——那是本地执行输出。当天 storm 打印 `opened 12`,而 `nextPositionId` 停在 12(最大只到 11),该仓根本没上链(D 批把 USDG 储备提空了,链上借不到)。此前战报中的仓位号因为都用 audit 波独立读链核对过,结论不受影响。**今后:凡"已开仓/已成交"类主张,一律以独立链上读为准。**
2. 委派要点见记忆 `codex_delegation_notes.md`:`-s workspace-write` 无网络;必须 `< /dev/null`;`-o` 只在结束时落盘;后台"任务完成"通知指包装 shell 退出,须用 `pgrep` 判断。
